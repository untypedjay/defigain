import styled from 'styled-components';

export const StyledBreadcrumb = styled.p`
  color: var(--clr-text);
  width: 400px;
`;

export const StyledBasepath = styled.span`
  color: var(--clr-disabled);
  
  a {
    color: var(--clr-disabled);
    text-decoration: none;
  }
`;